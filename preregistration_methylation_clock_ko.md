---
title: "사전등록: methylation 시계와 전사체 증식 읽기값의 관계"
date: "2026년 9월 16일"
---

이 문서는 GSE179847(Cellular Lifespan Study DNA methylation, EPIC array, 479개 시료)을 **내려받기 전에** 작성했다. SHA-256을 기록한 뒤 자료를 받고, 분석 스크립트는 실행 전에 해시를 확인한다. 고정 뒤에는 문서 끝의 변경 이력에만 추가한다.

# 1. 배경과 묻는 것

이 연구는 배양 섬유아세포에서 splicing factor를 비롯한 전사 프로그램이 **측정된 분열 속도**의 읽기값임을 보였다. methylation 시계가 배양에서 빨리 간다는 것(Sturm 2019)과 세포 분열이 후기 복제 영역의 메틸화 손실을 일으킨다는 것(Endicott 2022)은 이미 알려져 있다. 따라서 **"시계도 분열을 따라간다"는 것 자체는 새 질문이 아니다.**

새 질문은 하나다. **같은 배양에서 두 바이오마커 계열 — 전사체 증식 읽기값과 methylation 시계 — 이 같은 변수를 재고 있는가.** GSE179847과 GSE179848은 같은 세포주·같은 계대에서 나온 자료이므로 시료 수준에서 직접 짝지을 수 있다.

# 2. 자료

- **GSE179847**: `GSE179847_Cell_lifespan_DNAm_processed_matrix.csv.gz`(약 2.0 GB). 행은 CpG, 열은 시료별 beta와 Detection P value. 시료 식별자는 sentrix ID이며 GEO 시료 목록으로 GSM·`unique_variable_name`에 연결한다.
- **GSE179848**: 이미 받은 RNA-seq 자료. 328개 라이브러리에 측정된 분열 속도가 붙어 있다.
- 시계 계수: Horvath 2013 다조직 시계(353 CpG). skin & blood 시계(391)와 PhenoAge(513)는 계수표를 공개 출처에서 구할 수 있을 때만 쓰고, 구하지 못하면 그 사실을 적는다.

# 3. 고정하는 정의

- **DNAm age**: 각 시계의 공개 계수로 계산한다. Horvath 2013은 역변환(성인 기준)을 그대로 쓴다. 결측 CpG는 코호트 평균으로 대체하고 **대체한 개수를 보고한다**.
- **clock acceleration**: 같은 세포주 안에서 DNAm age를 배양 일수에 회귀한 잔차.
- **measured division rate**: `24 / population_doubling_time_uhours_per_division` (divisions/day). 다른 모든 자료에는 이 이름을 쓰지 않는다.
- **transcriptomic proliferation score**: 기존 20개 marker 평균 z (MKI67 등).
- **splicing score**: 기존 177개 pre-mRNA processing 집합의 평균 z.
- **짝짓기**: `unique_variable_name`이 정확히 같은 시료만 짝으로 본다. 정확 일치가 없으면 세포주·처리·산소·계대가 모두 같은 시료로 한정하고, 짝지은 수를 보고한다.
- **정규화 주의**: 이 분석은 원 논문의 파이프라인(BMIQ 등)을 재현하지 않으므로 **절대 나이 추정은 근사값이다.** 절대값은 해석하지 않고, 상관과 잔차만 해석한다.

# 4. 가설과 성공 기준

| | 가설 | 성공 기준 |
|---|---|---|
| **M1** (온전성 검사) | 무처리 건강 세포주에서 DNAm age는 누적 분열 수에 따라 증가한다 | ρ ≥ 0.3, *P* < 0.05 |
| **M2** | clock acceleration이 **측정된 분열 속도**와 연관된다 | \|ρ\| ≥ 0.3, *P* < 0.05 |
| **M3** (핵심) | 같은 배양에서 전사체 증식 점수가 DNAm age와 연관된다 | \|ρ\| ≥ 0.3, *P* < 0.05 |
| **M4** | splicing 점수와 DNAm age의 연관은 측정된 분열 속도를 보정하면 줄어든다 | 부분 상관이 50% 이상 감소 |

- M1이 실패하면 시계 계산이 잘못된 것으로 보고 **M2–M4를 해석하지 않는다.** 그 경우 실패를 그대로 적는다.
- 어느 가설이든 반대 방향이 나오면 그대로 보고한다. 결과를 본 뒤 기준을 바꾸지 않는다.
- 다중 검정은 네 가설에 대해 BH 보정한다.

# 5. 제외 기준

- Detection P value > 0.01인 측정은 결측으로 처리한다.
- 시계 CpG 중 30% 이상이 결측인 시료는 제외하고 그 수를 보고한다.
- 분열 속도가 없거나 0 이하인 시료는 제외한다(RNA 분석과 동일 규칙).

# 6. 보고 원칙

- 절대 DNAm age는 근사값임을 그림과 본문에 적는다.
- 이 분석은 논문에서 **패널 하나와 문단 하나**를 넘지 않는다. 주장은 "두 바이오마커 계열이 배양에서 같은 변수를 읽는다" 하나로 제한한다.
- Endicott 2022와 Sturm 2019를 선행 연구로 명시하고, 이 분석이 그것을 재확인하는 부분과 새로 더하는 부분을 구분해 적는다.

# 7. 변경 이력

(없음)
